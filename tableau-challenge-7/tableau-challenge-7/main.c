#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n;

    printf("Entrez le nombre d'elements : ");
    scanf("%d", &n);

    int tableau[n];

    for (int i = 0; i < n; i++)
    {
        printf("donner l'element %d :", i + 1);
        scanf("%d", &tableau[i]);
    }
    for(int i = 0; i < )
        return 0;
    }

