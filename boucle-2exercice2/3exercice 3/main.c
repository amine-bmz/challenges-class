#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main(){
  int n, prim = 1, i;
  printf("entre un nombre :");
  scanf("%d" , n);
  if (n <= 1){
    prim = 0;
  }
  else{
   for (i = 2; i <= sqrt(n); i++){
    if(n % i

    return 0;
}
